- name: demo on loops
  hosts: local
  connection: local
  tasks:
  - name: print name
    ansible.builtin.debug:
     msg: "{{ item }}" #Here item is the reserved keyword
    loop:
    - rahul
    - suresh
    - ramesh
    