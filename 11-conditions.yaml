- name: demo on conditions
  hosts: local
  connection: local
  vars:
    IS_LIVE: true
  tasks:
  - name: check is this true or not
    ansible.builtin.debug:
      msg: " yes it is live "
    when: IS_LIVE