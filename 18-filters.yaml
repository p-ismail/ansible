- name: demo on filters
  hosts: local
  connection: local
  vars:
    topics: linux,shell,ansible,terraform
  tasks:
  - name: convert the string into list
    ansible.builtin.debug:
      msg: "{{ topics | split (',') }}"