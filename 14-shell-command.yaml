- name: ansible command vs shell module
  hosts: local
  connection: local
  tasks:
  - name: command line module test
    ansible.builtin.command: cat /etc/passwd | grep -i root
    register: user_output

  # - name: print the user_output
  #   ansible.builtin.debug:
  #     msg: {{ user_output }}