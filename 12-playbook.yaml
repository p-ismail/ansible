- name: check the number is greater than 7 or not
  hosts: local
  connection: local
  vars:
    NUMBER_OF_SESSIONS: 7
  tasks:
  - name: check and print the number is greater than 7 or not
    ansible.builtin.debug:
      msg: " yes the number is greater than 5"
    when: NUMBER_OF_SESSIONS > 5